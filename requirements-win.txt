# Windows 全量依赖

# ── 引擎核心 ──
numpy==2.5.2
scipy==1.18.0
onnxruntime==1.29.0

# ── 网络/服务发现 ──
cryptography>=41.0.0
zeroconf==0.150.0
aiohttp==3.14.3

# ── 音频/媒体 ──
opuslib==3.0.1
av==18.1.0
miniaudio==1.71

# ── 工具 ──
importlib_metadata
packaging
protobuf
zipp

# ── 网络推流 ──
websockets==17.0.1

# ── 连接二维码 ──
qrcode==8.2

# ── 音频后端 ──
PyAudio==0.2.14

# ── Windows Core Audio 端点音量（VB-CABLE 音量条）──
# pycaw 依赖 comtypes（COM 互操作）+ psutil（仅 AudioSession 用，端点音量不需）
pycaw==20251023
comtypes==1.4.16
psutil==7.2.2

# ── 打包工具链 + 系统服务 ──
altgraph
pefile
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.6
pywin32
pywin32-ctypes
